<section class="search-results">
    {{#each .}}
    <article class="stop">
        <h1>{{name}}</h1>
        Linjer: <span class="lines">{{lineString}}</span>
    </article>
    {{/each}}
</section>
